/**
 * Use this type to temporary bypass to `any` without writting `any`
 * Comment the line to find where it's used
 */
// type TODO = any;

/**
 * Use this type to use an explicit `any`
 */
// type ExplicitAny = any;

// type UniqueIdentifier = number | string;

export {}